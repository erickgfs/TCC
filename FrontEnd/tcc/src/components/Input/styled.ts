import styled from 'styled-components';

export const Container = styled.div`
  background: #fafafa;
  padding: 16px;
  border-radius: 10px;
  border: 1px solid;
  align-self: center;
  margin: 15px 30px;

  input {
    flex: 1;
    background: transparent;
    border: none;
    font-size: 30px;
  }

  svg {
    margin-right: 16px;
  }
`;
